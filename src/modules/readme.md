This folder should contain context specific combinations of components.

For example, this is a good folder to host `<CloudContactForm/>`, but
not a good folder to host generic `<Input/>` (this one should go to
`../components`).
